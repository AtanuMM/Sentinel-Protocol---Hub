/** OpenAPI `tags` values; keep in sync with route `schema.tags`. */
export const openApiTags = {
  health: "Health",
  ftpIntegration: "FTP — integration",
  ftpProvisioning: "FTP — provisioning",
  ftpFeed: "FTP — feed",
  ftpIngestion: "FTP — ingestion",
  emailProvisioning: "Email — provisioning",
  emailIngestion: "Email — ingestion",
} as const;
